# Dependências do Node (apenas para testes locais)
node_modules/
package-lock.json

# Logs e arquivos de sistema
*.log
.DS_Store
Thumbs.db

# Dados de sessão (ficam apenas no servidor Railway, não no Git)
# presencas.json — não ignorar, Railway precisa do arquivo inicial vazio
# assembleias.json — não ignorar, Railway precisa do arquivo inicial
