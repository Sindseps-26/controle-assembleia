# Dependências do Node (apenas para testes locais)
node_modules/
package-lock.json

# Logs e arquivos de sistema
*.log
.DS_Store
Thumbs.db

# Dados de sessão em tempo real — gerados pelo servidor, não versionados
# Os arquivos .example abaixo são os modelos iniciais para o Railway
presencas.json
presencas_backup_*.json
sessao.json
cpfs.json
cpfs_backup_*.json

# assembleias.json contém dados reais de participantes — não versionar
# Use assembleias.example.json como modelo inicial no servidor
assembleias.json

# Arquivo de teste — não deve ir para produção
test_qr.html

# Variáveis de ambiente locais (TOKEN_ADMIN, etc.) — NUNCA versionar
.env
.env.local
